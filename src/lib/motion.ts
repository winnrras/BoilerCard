// Apple-style "ease-out expo" curve, reused for every animation in the app
// so motion reads as one consistent system rather than per-component defaults.
export const EASE = [0.16, 1, 0.3, 1] as const;
